# KnowledgeBase Creator Configuration

# Required for dump step (skip with -SkipDump).
STUDIO_PRO_PATH=C:\Program Files\Mendix\10.24.15.93102
MPR_FILE_PATH=C:\Workspaces\Mendix\Smart Expenses app-main\SmartExpenses.mpr
APP_NAME=SmartExpenses

# Optional explicit mx.exe path override.
# MENDIX_MX_EXE=C:\Program Files\Mendix\10.24.15.93102\modeler\mx.exe

# Optional output root (default: ../mendix-data).
MENDIX_DATA_ROOT=

# Optional module filter for parser. Use * for all modules.
MENDIX_MODULES=*

# Optional strict mode. false keeps running and prints warnings on gate failures.
STRICT_MODE=false

# Optional app-specific benchmark scenarios JSON file.
# CUSTOM_SCENARIOS_PATH=benchmarks/smartexpenses-custom-scenarios.json

# Optional path to an existing dump JSON when using -SkipDump without -SkipParser.
# DUMP_FILE_PATH=
