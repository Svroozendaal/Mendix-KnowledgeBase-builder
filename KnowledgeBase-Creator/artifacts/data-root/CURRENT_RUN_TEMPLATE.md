# Current Run

- App: `{{APP_NAME}}`
- Generated at: `{{GENERATED_AT_UTC}}`
- Active overview export: `{{RUN_FOLDER}}`
- Active knowledge base: `{{KB_ROOT}}`
- Recommended entry point: `{{KB_READER}}`

## Escalation Order

1. Start in `{{KB_ROOT}}`
2. Escalate to `{{RUN_FOLDER}}` when the KB is incomplete or you need export-backed proof
3. Escalate to `{{DUMPS_ROOT}}` only for parser-gap investigation
